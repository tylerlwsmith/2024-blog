{
  "name": "webapp",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^3.2.1",
    "prettier-plugin-go-template": "^0.0.15"
  }
}
